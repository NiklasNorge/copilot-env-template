## Usage

Keep examples minimal and code-backed.

```python
result = transform_orders(df, strict=True)
print(result.shape)
```

Document assumptions next to examples:
- expected input columns
- key validation rules
- important defaults
